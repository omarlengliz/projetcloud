<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Quotes App</title>
</head>
<body>
  <h1>Quotes</h1>
  <p>Served by: <%= vmName %></p>
  <ul>
    <% quotes.forEach(quote => { %>
      <li>
        <%= quote.title %>
          <audio controls>
            <source src="/audio/<%= quote.id %>.mp3" type="audio/mpeg">
            Your browser does not support the audio element.
          </audio>
        
      </li>
    <% }); %>
  </ul>
</body>
</html>
